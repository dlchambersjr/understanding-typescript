{
    "compilerOptions": {
        "module": "commonjs",
        "target": "es5",
        "noImplicitAny": false,
        "sourceMap": false, // Adds .tsx file in browser for debugging
        "noEmitOnError": true // Dpes not creat .js if errors
    },
    "exclude": [
        "node_modules"
    ]
}
